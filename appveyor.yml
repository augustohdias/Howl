build: off

version: 0.0.1.{build}

image:
  - Visual Studio 2017

branches:
  only:
    - master

for:
  -
    matrix:
      only:
        - image: Visual Studio 2017
    
    environment:
      STACK_ROOT: 'C:\sr'
      TMP: 'C:\tmp'
      OS: windows

    cache:
      - '%STACK_ROOT%'
      - 'C:\Users\appveyor\AppData\Local\Programs\stack'
